#!/bin/sh
# Agent provenance is REQUIRED here (AGENTS.md > Commit messages). This hook used to
# strip agent trailers; the operator lifted that ban on 2026-08-28 because with two
# agents in one tree and a shared git author, the trailer is the only per-commit
# signal of who did the work.
#
# Nothing is injected automatically: a hook cannot know which agent is committing, and
# a guessed trailer is worse than none. Agents add their own; see commit-msg.
exit 0
