---
channel: tapin
tags: [facts, operator, research]
topic: REPLACE - the topic these facts belong to
date: YYYY-MM-DD
tier: operator
verified_at: YYYY-MM-DD
source: hand-entered (who/where you checked)
---

# Operator facts - REPLACE topic

<!--
Copy to: <vault>/<channel>/_operator_facts/<date>_<topic>.md

Why the frontmatter matters (core/fact_store.py):
  tier:        operator > link > web > signal > vault > brief > context.
               `operator` is the highest-trust tier and WINS fact conflicts, so
               only use it for something you personally verified.
  verified_at: drives freshness ranking. A note without it ranks below dated ones.
  expires:     optional (YYYY-MM-DD). Expired notes are dropped, not down-ranked -
               use it for anything time-boxed (rankings, "current champion", odds).

One claim per bullet. Keep each self-contained: the script prompt sees the line,
not this file's title.
-->

- REPLACE - one verified claim, with its number/date inline
- REPLACE - another
