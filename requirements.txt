# Content Machine — runtime dependencies.
#
# NOTE: pyproject.toml is the canonical dependency source. This file is kept
# in sync for `pip install -r requirements.txt` compatibility. Prefer:
#     pip install -e .           # runtime
#     pip install -e ".[dev]"    # + ruff/mypy/pytest/pre-commit
#
# System dependency (not pip): ffmpeg on PATH. Python: 3.10+.
python-dotenv==1.0.1

openai==2.33.0
google-api-python-client==2.196.0
google-auth==2.52.0
requests==2.32.3
beautifulsoup4==4.14.3
moviepy==1.0.3
imageio-ffmpeg==0.6.0
elevenlabs==2.45.0
Pillow==9.5.0
numpy==2.4.4
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.1
tzdata>=2024.1; platform_system == "Windows"

# Optional sports stats (nflverse / pybaseball — install for NFL/MLB stat lines)
# nfl_data_py>=0.3.0
# pybaseball>=2.2.7

google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.0
